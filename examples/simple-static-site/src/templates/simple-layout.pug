block variables

<!doctype html>
html
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    title #{title ? title + ' | ' : ''}#{env.APP_TITLE}
    link(rel='icon' href=env.APP_BASE_URL + '/favicon.ico')
  body
    nav
      a(href=env.APP_BASE_URL + '/') Home
      a(href=env.APP_BASE_URL + '/blog/') Blog
      a(href=env.APP_BASE_URL + '/projects/') Projects

    div
      block content

    script(type='module' src='/index.js')
