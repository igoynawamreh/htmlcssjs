block variables

<!doctype html>
html
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    title #{title ? title + ' | ' : ''}#{env.APP_TITLE}
    link(rel='icon' href=env.APP_BASE_URL + '/favicon.ico')
  body
    block content

    script(type='module' src='/index.js')
